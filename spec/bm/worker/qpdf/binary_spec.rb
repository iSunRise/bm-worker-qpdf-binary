RSpec.describe Bm::Worker::Qpdf::Binary do
  it "has a version number" do
    expect(Bm::Worker::Qpdf::Binary::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
